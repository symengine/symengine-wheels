{% set version = "0.11.0" %}
{% set cxx_version = "0.11.1" %}

package:
  name: symengine-wheels
  version: {{ version }}

source:
  - url: https://github.com/symengine/symengine.py/archive/f09067861cbd99538508932e57ac29191a80eaa1.tar.gz
    folder: python

  - url: https://github.com/symengine/symengine/archive/ed1e3e4fd8260097fa25aa1282e1d3a4ac4527f3.tar.gz
    folder: cxx

build:
  number: 0
  ignore_run_exports_from:
    - gmp
    - mpfr
    - mpc
    - libflint
    - llvmdev
    - zlib
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}

requirements:
  build:
    - cmake
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - make   # [unix]
    - ninja  # [win]
    - patchelf        # [linux]
    - python          # [build_platform != target_platform]
    - cross-python_{{ target_platform }}      # [build_platform != target_platform]
    - setuptools      # [build_platform != target_platform]
    - auditwheel      # [linux]
    - delocate        # [win or osx]
    - unzip           # [unix]
    - m2-unzip        # [win]
    - cython >=0.29.24
  host:
    - gmp           # [unix]
    - mpir          # [win]
    - mpfr
    - mpc
    - libflint 2.* *nontl*
    - llvmdev <17
    - zlib
    - python
    - setuptools
    - cython >=0.29.24  # [build_platform == target_platform]
    - pip
  run:
    - python

test:
  requires:
    - pytest
    - sympy
    - numpy

about:
  home: https://github.com/symengine/symengine.py
  license: MIT
  license_file: python/LICENSE
  summary: Python wrappers for SymEngine, a fast symbolic manipulation library, written in C++

extra:
  recipe-maintainers:
    - isuruf
    - certik
